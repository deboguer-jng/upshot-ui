import sizes, { getSize } from './sizes'

const spinners = {
  sizes: {
    xs: getSize(4),
    sm: getSize(8),
    md: getSize(16),
    lg: getSize(24),
    xl: getSize(48),
  },
}

export default spinners
